# Captain test suites — one per test-bearing package. The RWX gate (.rwx/ci.yml)
# runs `captain run greenroom-<pkg>` per package; Captain runs the command, parses
# vitest's JSON, emits rwx-v1-json (surfaced inline in the RWX UI via the task's
# `outputs.test-results`), and retries only failed tests.
#
# `bunx vp` is the resolver-prefixed form (the doc's `npx vitest` equivalent) —
# Captain execs the command directly, so a bare `vp` isn't on PATH. `--root <dir>`
# scopes the run to one package from the repo root.
test-suites:
  greenroom-sprout:
    command: bunx vp test run --root workers/sprout --reporter=default --reporter=json --outputFile=../../.captain-out/sprout.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/sprout.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/sprout.rwx.json
    retries:
      attempts: 2
      command: bunx vp test run --root workers/sprout '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/sprout.vitest.json

  greenroom-bouncer:
    command: npx vp test run --root workers/bouncer --reporter=default --reporter=json --outputFile=../../.captain-out/bouncer.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/bouncer.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/bouncer.rwx.json
    retries:
      attempts: 2
      command: npx vp test run --root workers/bouncer '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/bouncer.vitest.json

  greenroom-guestlist:
    command: npx vp test run --root workers/guestlist --reporter=default --reporter=json --outputFile=../../.captain-out/guestlist.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/guestlist.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/guestlist.rwx.json
    retries:
      attempts: 2
      command: npx vp test run --root workers/guestlist '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/guestlist.vitest.json

  greenroom-promoter:
    command: bunx vp test run --root workers/promoter --reporter=default --reporter=json --outputFile=../../.captain-out/promoter.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/promoter.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/promoter.rwx.json
    retries:
      attempts: 2
      command: bunx vp test run --root workers/promoter '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/promoter.vitest.json

  greenroom-roadie:
    command: npx vp test run --root workers/roadie --reporter=default --reporter=json --outputFile=../../.captain-out/roadie.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/roadie.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/roadie.rwx.json
    retries:
      attempts: 2
      command: npx vp test run --root workers/roadie '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/roadie.vitest.json

  greenroom-kit:
    command: bunx vp test run --root packages/kit --reporter=default --reporter=json --outputFile=../../.captain-out/kit.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/kit.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/kit.rwx.json
    retries:
      attempts: 2
      command: bunx vp test run --root packages/kit '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/kit.vitest.json

  greenroom-secrets:
    command: bunx vp test run --root packages/secrets --reporter=default --reporter=json --outputFile=../../.captain-out/secrets.vitest.json
    results:
      language: JavaScript
      framework: Vitest
      path: .captain-out/secrets.vitest.json
    output:
      reporters:
        rwx-v1-json: .captain-out/secrets.rwx.json
    retries:
      attempts: 2
      command: bunx vp test run --root packages/secrets '{{ file }}' -t '{{ testNamePattern }}' --reporter=default --reporter=json --outputFile=../../.captain-out/secrets.vitest.json
