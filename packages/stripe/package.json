{
  "name": "@si/stripe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./gate": "./src/gate.ts"
  },
  "scripts": {
    "env:init": "test -f src/generated.ts || bun run scripts/fetch.ts",
    "fetch": "bun run scripts/fetch.ts",
    "fetch:strict": "bun run scripts/fetch.ts --strict",
    "sync": "bun run scripts/sync.ts",
    "validate": "bun run scripts/validate.ts",
    "test": "vp test run"
  },
  "dependencies": {
    "stripe": "catalog:"
  },
  "devDependencies": {
    "@si/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "vite-plus": "catalog:"
  }
}
