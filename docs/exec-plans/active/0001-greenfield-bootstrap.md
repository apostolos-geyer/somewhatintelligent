# 0001 — Greenfield bootstrap of the somewhatintelligent platform

Status: ACTIVE
Started: 2026-07-07
Base: verbatim import of greenroom@075f3bf (commit 723d40a)

## Why

The `somewhatintelligent` Instagram account needs a store (a shirt went
viral) and, behind it, a platform: a simple subscription system that can
later gate more apps, using the IG account as the funnel. This repo is the
new home for all somewhatintelligent infrastructure. It inherits the most
active platform spine (greenroom), borrows the storefront (apostoli.ca),
the Stripe IaC pattern (HiPat.app), and the mail app (agentic-inbox) —
pruning dead weight and applying accumulated harness learnings at each step.

## Target state

- **Apps**: `workers/store` (storefront, `/shop`) + `workers/identity`
  (`/account`) behind `workers/bouncer` on `somewhatintelligent.ca`.
- **Routing** (bouncer `vars.ROUTES`, single host per env):
  - `/api` → guestlist, `/account` → identity, `/shop` → store,
    `/` → redirect to `/shop`.
  - Staging mirror on `staging.somewhatintelligent.ca`, protected by
    Cloudflare Access.
- **Mail**: vendored agentic-inbox SI instance at
  `mail.somewhatintelligent.ca` (worker `agentic-inbox-si`), deploys
  separately, NOT integrated with platform auth — Cloudflare Access only.
- **Stripe**: `packages/stripe` IaC (HiPat pattern: metadata-tagged
  idempotent sync, offline stub generation). One tier: `member`
  (`member_monthly`, placeholder price). `@better-auth/stripe` wired in the
  auth factory but enabled only when `STRIPE_SECRET_KEY` exists.
  **No storefront↔Stripe integration yet** — checkout keeps the manual
  pending/paid stub.
- **Provisioning**: idempotent Cloudflare SDK scripts (`scripts/provision/`)
  for tokens, D1, R2+CORS, Access apps + service token, Email Routing,
  staging test users. Input: an API token with token-creation perms + account id.
- **CI/CD**: RWX (greenroom lanes renamed), scaffolded but inert until the
  RWX GitHub App + vaults are configured (runbook in docs/ops). Per-worker
  GitHub deploy records carry Cloudflare-dashboard and live-URL links.
- **Version strings** rendered in every app (UI footer) and `/__version`
  on API workers.
- **Design system**: monochrome + rounded, zero box-shadow/blur; depth via
  solid/dashed/dotted borders — blueprint/diagram aesthetic. Iosevka stays.

## Decision log

| #   | Decision                                                                                                                                                                                                                                                                                                                                                          | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Verbatim import first, prune second                                                                                                                                                                                                                                                                                                                               | User-specified; keeps a reviewable trail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2   | Iosevka fonts kept in full                                                                                                                                                                                                                                                                                                                                        | Owner uses the font; fits blueprint aesthetic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3   | No Stripe in storefront checkout yet                                                                                                                                                                                                                                                                                                                              | Owner will handle products/pricing separately                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 4   | Apex `/` redirects to `/shop`; `marketing` worker pruned                                                                                                                                                                                                                                                                                                          | Default (owner may override); IG-funnel store needs no marketing site                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 5   | Org/multi-tenancy plugin stays wired                                                                                                                                                                                                                                                                                                                              | Default; stripping is schema surgery for no near-term gain                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 6   | Package scope `@si/*` → `@si/*`, workerPrefix `si`                                                                                                                                                                                                                                                                                                                | Short, collision-free                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 7   | Cloudflare account `c735c5a53d864bee37400befb7f4c7f4` (personal); zone `somewhatintelligent.ca` = `777506a7cc42ec22ffafce16b3d36d06` (active)                                                                                                                                                                                                                     | Verified live 2026-07-07                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8   | Root `vp test` shows 39 file-level parse failures with all tests passing — identical in greenroom source                                                                                                                                                                                                                                                          | Pre-existing quirk; per-package suites (CI gate) are the reference: 414 passing at baseline                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 9   | Identity is mounted at `/account` via bouncer's **existing `vmf` mode** (`handleMountedApp` in `workers/bouncer/src/proxy.ts`), NOT via an app-level TanStack Start basepath                                                                                                                                                                                      | First attempt (basepath in `vite.config.ts` + `router.basepath`) was investigated and found to collide `IDENTITY_URL`'s two uses (same-host `/api` SSR base vs. identity's own public `/account` address) across ~10 files with high silent-failure risk; owner redirected to vmf mid-task. vmf keeps identity's own code path-agnostic (bouncer strips `/account` inbound, rewrites asset/redirect/cookie paths outbound) and is the same mechanism the future `/shop` store mount will use.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 10  | Bouncer's "one mode per host" boot-time rule relaxed to "one mode per **(host, mount)**"                                                                                                                                                                                                                                                                          | The single-host apex now legitimately mixes `/api` passthrough + `/account` vmf + `/` redirect on ONE host. Dispatch (`index.ts`) already picks mode per matched route; `handleMountedApp` only ever rewrites the response for the mount it matched, so different mounts in different modes on one host don't conflict. Only the SAME mount declared in two modes is still rejected (genuinely ambiguous).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11  | **Known, accepted gap**: vmf cannot rewrite identity's client-side SPA navigation                                                                                                                                                                                                                                                                                 | `AllAttributesRewriter` (proxy.ts) only rewrites HTML attrs matching `ASSET_PREFIXES` (or favicon links) — plain navigational `<a href="/sign-in">`/TanStack `<Link>` output and the router's client-side history/pushState are untouched, since TanStack Router has no basepath in this design. First page load under `/account` renders correctly (assets, initial HTML, and any HTTP redirect Location all get rewritten); after hydration, clicking an in-app `<Link>` updates the browser URL bar to the ROOT path (e.g. `/sign-in`, dropping `/account`), and a hard refresh/deep-link at that bare URL then hits bouncer's `/` catch-all (redirects to `/shop`) instead of identity. `resolveReturnTo`'s relative-path passthrough (`workers/identity/src/lib/return-to.ts`) has the same root cause. The clean fix is giving identity a real basepath (decision #9's rejected first attempt) or forcing every identity nav through a full page load (impractical) — neither attempted here; flagged for P2/P3 if it proves user-visible in the browser walk. |
| 12  | `IDENTITY_URL` now means "identity's own public address" (`<origin>/account` in staging/production) for cross-app links (guestlist's `identityUrl` → login/consent/invite-accept links; identity's own sidebar/invite-modal); `BETTER_AUTH_URL` stays the bare same-host ORIGIN (no `/account`, no `/api`) since better-auth appends its own `/api/auth` basePath | Two different needs on one historical env-var name; `workers/identity/src/lib/auth-client.ts`'s SSR fallback derives the bare origin via `new URL(IDENTITY_URL).origin` rather than reusing the value directly, since `/api` lives at the origin root, not under `/account`. Local dev (`.dev.vars`, dev-direct, no bouncer) is unaffected — `IDENTITY_URL` there is still identity's own plain dev origin.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 13  | D1 `database_id`s are `00000000-0000-0000-0000-000000000000` placeholders (guestlist + roadie, staging + production) with `// TODO(provision)` comments                                                                                                                                                                                                           | greenroom's account-scoped UUIDs are invalid on this fork's Cloudflare account; local dev/tests key on `database_name`, not the id, so this doesn't block P1 gates. Real ids land in P3 (live staging provisioning).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Decision log — Track E (RWX / release engineering + version strings)

Track E is **done** (2026-07-07): lanes re-homed, store folded into every
enumeration, per-worker deploy visibility, `/__version` across the fleet,
activation + scoping docs. CI remains **scaffolding-only** until the RWX
GitHub App + `si_deploy`/`si_preview` vaults exist —
`docs/ops/rwx-setup.md` is the activation runbook.

| #   | Decision                                                                                                                                                                                                                                                                                                                                                        | Why                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | **inbox has NO CI/CD lane and NO release-please component** — `inbox/**` is a changed-files no-op everywhere; it deploys manually (`cd inbox && bun run deploy`) and is versioned informally. `inbox` stays a valid commit SCOPE.                                                                                                                               | Owner correction mid-track (initial cut had wired an inbox lane; removed)                                                                                                                                                                           |
| E2  | Per-worker GitHub Deployments: one record per shipped worker in environment `<env>/<worker>` (immediate `success`, environment_url = live URL else CF dashboard link, payload carries version/dashboard/live/RWX-run) ON TOP of the run-level fleet record, plus ONE marker-upserted PR/commit comment (`deployed <w>@<v> to <env>: <live> \| <dash>` per line) | Owner: "deploys per worker with links to cf dashboard and to the url if any in gh, not just in releases"; best-effort like the pre-existing script — bookkeeping can never fail a green deploy                                                      |
| E3  | Version strings: wrangler-built workers get `WORKER_VERSION`/`WORKER_COMMIT` injected as `--var` AT SHIP TIME (deploy-worker.sh + preview uploads, so promoted versions carry them) read by the shared `@si/kit/version` `/__version` helper; identity bakes build-time constants via vite `define` (inbox pattern) + UI footer                                 | `wrangler.jsonc` `define`/vars can't compute at build; `--var` rides both `wrangler deploy` AND `versions upload` so every lane (incl. promote-over-rebuild) reports true values; kit fallbacks keep local dev/tests green with zero Env-type churn |
| E4  | Bouncer answers `/__version` BEFORE route matching; guestlist answers `/__version` + `/api/__version`                                                                                                                                                                                                                                                           | Works on every fronted host (incl. hosts whose `/` is a redirect); bouncer's `/api` mount is passthrough (prefix NOT stripped), so guestlist must recognize the mounted spelling — identity needs nothing extra (vmf strips `/account`)             |
| E5  | Identity build is BROKEN at `og:build` on this branch (`packages/og/src/fonts.ts` loads `@si/design/fonts/boska/*` that Track F deleted) — pre-existing, not fixed in Track E                                                                                                                                                                                   | Out of scope (packages/og is Track F fallout); `vp build` (the vite build itself, incl. the new defines) verified green directly; must be fixed before any identity deploy/preview can succeed                                                      |
| E6  | `scripts/staged-check.ts` now routes non-TS files (yml/sh/md/json) to format-only                                                                                                                                                                                                                                                                               | A CI-config-only commit made `vp check` fail with "Expected at least one target file", vetoing the commit; the hook stays mandatory (no `--no-verify`)                                                                                              |

## Decision log — Track F (design system, executed by the main session directly)

| #   | Decision                                                                                                                                                                                                     | Why                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| F1  | "DRAFT" blueprint system: warm graphite ink on drafting paper, inverted dark board; zero box-shadow blur, zero backdrop-filter; depth = border grammar (solid/dashed/dotted) + hard `--brutal-*` ink offsets | Owner brief: monochrome, rounded, no soft shadows/blur, lines for depth |
| F2  | Accent slots renamed sprout/stigma/growth/pistil/haze → ink/rust/success/warning/info; values monochrome except rust (danger) + muted approval green (success) per owner                                     | "success can still be green and bad can still be red"                   |
| F3  | Typography = Iosevka Aile (display/body/editorial) + Iosevka (mono/accent); legacy brand faces (Zerove/Switzer/IBM Plex/Quadrillion/Boska/Literata) and 8.6M of Sprout texture PNGs deleted                  | Iosevka stays (owner); dead weight cut                                  |
| F4  | New mark: drafting registration symbol (circle + cardinal ticks + plotted center), hook-free satori-safe SVG; wordmark lowercase Aile                                                                        | Replaces the sprout leaf                                                |
| F5  | Shadow families keep names, values hardened to 0-blur (`--soft-*` = hard rules, `--neo-*` = chisel, glass = opaque sheet); `--shadow-brand` = `--brutal-md`                                                  | API stability for consumers; no soft rendering possible                 |
| F6  | agent-browser `fill` does not drive TanStack Form state — sign-in must use `keyboard type` + blur; encoded in interactive-test skill                                                                         | Diagnosed live; API was healthy, form validated empty state silently    |
| F7  | Contrast: `audit:contrast` 78 pairs, 0 WCAG-AA failures (70 AAA)                                                                                                                                             | Gate kept green                                                         |

## Phases

- **P0** (done when this doc lands): verbatim import, green baseline
  (typecheck 15/15, per-package tests 7/7 suites), pushed.
- **P1** (done): rebrand (brand.ts / deploy.ts / app-brand.ts / `@si/*` scope) +
  prune (sprout, marketing, docs/sprout, e2e/sprout, marketing-videos,
  RealtimeKit, PDFs) + bouncer route table rewrite (`/api` passthrough,
  `/account` vmf, `/` redirect — see decisions #9–#13 for the mid-flight
  direction change from an app-level basepath to bouncer's `vmf` mount, the
  mode-consistency relaxation, and the one known/accepted gap). Gates green
  after.
- **P2** parallel tracks (worktree-isolated agents; merge small→large,
  design last): A storefront port · B Stripe IaC · C inbox vendor ·
  D provisioning suite · E RWX/release engineering + version strings
  (**done 2026-07-07** — see Track E decision log; CI scaffolding-only
  until `docs/ops/rwx-setup.md` is executed) · F design system.
- **P3**: live staging provisioning + deploy (fleet order: promoter roadie
  guestlist identity store bouncer; + inbox), Access, seed users, smoke +
  browser walk.
- **P4**: harness docs rewrite (commit scoping for per-worker
  release-please, RWX activation checklist), final gates, push.

## Commit conventions (release-please)

Squash-merge titles and commits MUST be conventional commits scoped to the
worker they touch (`feat(store): …`, `fix(bouncer): …`) — release-please
versions each worker from those scopes. Cross-cutting changes use the most
affected worker's scope or `chore(repo): …`.
