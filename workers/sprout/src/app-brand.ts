/**
 * Per-app brand constants for the sprout app. Platform-wide brand
 * (wordmark name, cookie prefix, providerId) lives in `@greenroom/config`.
 * Anything app-specific — like the product name shown after the wordmark
 * in this app — lives here.
 */
export const APP_PRODUCT_NAME = "Sprout";
