/**
 * Per-app brand constants for the store app. Platform-wide brand (wordmark
 * name, cookie prefix, providerId) lives in `@si/config`. Anything app-specific
 * — the product name shown after the wordmark, the storefront tagline — lives
 * here.
 */
export const APP_PRODUCT_NAME = "somewhatintelligent shop";

// The storefront's own copy. Brand NAME comes from @si/config
// (platformConfig.brand.name); this is the store's positioning line.
export const STORE_TAGLINE = "Wearable field notes. Printed in small runs.";
