# promoter

Queue consumer for outbound email (Resend) and scheduled jobs.

## Releases

Released independently as component tags `promoter-v<x.y.z>` (release-please manifest mode; see `docs/runbooks/PRODUCTION-DEPLOY.md`).
