# bouncer

Edge ingress: fronts every production host, verifies sessions with guestlist, and stamps signed request envelopes for downstream apps.

## Releases

Released independently as component tags `bouncer-v<x.y.z>` (release-please manifest mode; see `docs/runbooks/PRODUCTION-DEPLOY.md`).
